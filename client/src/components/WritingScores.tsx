import { Box } from "@mui/system";
import { ScoreProps } from "../types";

export default function WritingScores(p: ScoreProps) {
    return (
        <Box>
            <p>{"writing"}</p>
        </Box>
    )
}
