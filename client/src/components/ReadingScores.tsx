import { Box } from "@mui/system";
import { ScoreProps } from "../types";

export default function ReadingScores(p: ScoreProps) {
    return (
        <Box>
            <p>{"reading"}</p>
        </Box>
    )
}
